name: Deploy
# One workflow, both environments. Push auto-deploys dev; prod is a manual
# promotion (Actions → Run workflow). No waiting/polling job, so nothing logs
# or burns minutes while a manual approval is pending — promote whenever.
#
# A manual dispatch has no commit message in context, so the run name says
# "Promote to prod" and the promotion-info job prints the commit + PR.
run-name: "${{ github.event_name == 'workflow_dispatch' && format('Promote to prod — @{0}', github.actor) || format('Deploy dev: {0} (@{1})', github.event.head_commit.message, github.actor) }}"

on:
  push:
    branches: [main]
  workflow_dispatch: # = promote current main to prod

permissions:
  contents: read

# The frontend bakes VITE_API_BASE_URL in at build time, so (unlike the API)
# each environment is built separately with its own API URL and Entra config.
jobs:
  # On a manual promote, spell out in the run Summary exactly which commit and
  # PR are going to prod (the run name can't carry the message on a dispatch).
  promotion-info:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Summarise what is being promoted
        env:
          GH_TOKEN: ${{ github.token }}
        # Purely informational: never fail the promotion over the summary. The
        # API calls can transiently return nothing (e.g. right after a merge),
        # so tolerate errors (set +e) and fall back rather than exit non-zero.
        run: |
          set +e
          commit="${{ github.sha }}"
          subject=$(gh api "repos/${{ github.repository }}/commits/$commit" --jq '.commit.message' 2>/dev/null | head -1)
          pr=$(gh api "repos/${{ github.repository }}/commits/$commit/pulls" \
                 --jq 'map("#\(.number) — \(.title)") | first // empty' 2>/dev/null)
          {
            echo "## 🚀 Promoting to production"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| Commit | \`$commit\` |"
            echo "| Message | ${subject:-(unavailable)} |"
            echo "| PR | ${pr:-(none found)} |"
          } >> "$GITHUB_STEP_SUMMARY"

  # Auto — every push to main builds and deploys to dev. Skipped on a manual run.
  dev:
    if: ${{ github.event_name == 'push' }}
    uses: ./.github/workflows/deploy-env.yml
    with:
      environment: dev
    secrets: inherit

  # Manual — promote to prod by running this workflow (Actions → Run workflow).
  # Skipped (greyed) on push runs, so it's clearly the gated stage without any
  # job sitting and polling for approval.
  prod:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    uses: ./.github/workflows/deploy-env.yml
    with:
      environment: prod
    secrets: inherit
