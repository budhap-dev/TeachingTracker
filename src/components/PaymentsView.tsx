/**
 * @deprecated Renamed to {@link StudySnapshotView} — this component renders the
 * "Study snapshot" table, not payments. Kept as a re-export for backwards
 * compatibility; import `StudySnapshotView` directly in new code. Safe to
 * delete once the old `PaymentsView` test files are removed.
 */
export { StudySnapshotView as PaymentsView } from './StudySnapshotView'
