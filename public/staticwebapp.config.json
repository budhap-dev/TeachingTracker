{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
            "/assets/*",
            "/icons/*",
            "/*.{css,js,json,png,jpg,jpeg,gif,svg,ico,woff,woff2,webmanifest,xml,txt}"
        ]
    }
}
